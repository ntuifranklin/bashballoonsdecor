name: Docker-Testing

on: 
  push:
    branches:
      - '**'
    

jobs:
  build-website:
    runs-on: node:12.18-alpine
    steps:
      - uses: actions/checkout@v2
      - name: Test Bash Balloons Rentals Website/WebApp
        run: |
          npm install .
          npm run test
  
  